# 场景模板库

## 电商类 (E-commerce)

### 底部导航栏 (Tab Bar)
```
首页(home) | 分类(category) | 购物车(cart) | 消息(message) | 我的(profile)
```

### 金刚区 (Quick Actions)
```
限时抢购(flash-sale) | 优惠券(coupon) | 领积分(points) | 拼团(group-buy)
新品首发(new-arrival) | 直播(live) | 充值(recharge) | 会员(member)
```

### 商品详情 (Product Detail)
```
分享(share) | 收藏(favorite) | 客服(service) | 购物车(cart)
评价(review) | 店铺(shop) | 相似(similar) | 退货(return)
```

### 订单流程 (Order Flow)
```
待付款(pending-payment) | 待发货(pending-ship) | 待收货(pending-receive)
已完成(completed) | 已取消(cancelled) | 退款中(refunding)
```

---

## 社交类 (Social)

### 底部导航栏
```
首页(home) | 发现(discover) | 发布(publish/+) | 消息(message) | 我的(profile)
```

### 消息类型
```
聊天(chat) | 朋友圈(moments) | 群组(group) | 通知(notification)
系统消息(system) | @提及(mention)
```

### 互动功能
```
点赞(like) | 评论(comment) | 分享(share) | 转发(forward)
关注(follow) | 收藏(bookmark) | 拍照(camera) | 定位(location)
```

---

## SaaS/工具类 (SaaS / Productivity)

### 侧边栏导航
```
仪表板(dashboard) | 项目(projects) | 任务(tasks) | 团队(team)
日历(calendar) | 文档(docs) | 报表(reports) | 设置(settings)
```

### 通用功能
```
搜索(search) | 筛选(filter) | 排序(sort) | 导出(export)
导入(import) | 刷新(refresh) | 下载(download) | 上传(upload)
编辑(edit) | 删除(delete) | 复制(copy) | 粘贴(paste)
```

### 状态指示
```
成功(success) | 错误(error) | 警告(warning) | 信息(info)
加载中(loading) | 空状态(empty) | 离线(offline) | 同步(sync)
```

---

## 金融类 (Finance)

### 底部导航栏
```
首页(home) | 资产(assets) | 行情(market) | 交易(trade) | 我的(profile)
```

### 资产管理
```
总资产(total-assets) | 收入(income) | 支出(expense) | 转账(transfer)
账单(bills) | 预算(budget) | 理财(investment) | 借贷(loan)
```

### 交易相关
```
买入(buy) | 卖出(sell) | 充值(deposit) | 提现(withdraw)
红包(red-packet) | 优惠券(coupon) | 积分(points) | 银行卡(bank-card)
```

---

## 健康/运动类 (Health & Fitness)

### 底部导航栏
```
首页(home) | 运动(sport) | 发现(discover) | 数据(data) | 我的(profile)
```

### 运动类型
```
跑步(running) | 骑行(cycling) | 游泳(swimming) | 瑜伽(yoga)
力量训练(strength) | 冥想(meditation) | 睡眠(sleep) | 步行(walking)
```

### 健康数据
```
心率(heart-rate) | 血压(blood-pressure) | 体重(weight)
卡路里(calories) | 步数(steps) | 距离(distance) | 时长(duration)
```

---

## 教育类 (Education)

### 底部导航栏
```
首页(home) | 课程(courses) | 学习(learn) | 消息(message) | 我的(profile)
```

### 学习功能
```
视频课(video-course) | 直播课(live-course) | 音频课(audio-course)
题库(question-bank) | 考试(exam) | 证书(certificate) | 笔记(notes)
```

### 互动功能
```
提问(ask) | 答疑(qa) | 讨论(discuss) | 作业(homework)
排名(ranking) | 成就(achievement) | 打卡(check-in) | 连续天数(streak)
```

---

## 游戏类 (Gaming)

### 通用功能
```
开始(start) | 暂停(pause) | 设置(settings) | 排行榜(leaderboard)
商店(shop) | 背包(inventory) | 任务(quest) | 成就(achievement)
```

### 游戏元素
```
金币(gold-coin) | 钻石(diamond) | 体力(energy) | 经验(exp)
武器(weapon) | 盾牌(shield) | 药水(potion) | 宝箱(chest)
```

### 社交功能
```
好友(friends) | 公会(guild) | 聊天(chat) | 邮件(mail)
组队(team-up) | PVP | PVE | 分享(share)
```

---

## 通用图标集 (Universal Icons)

### 导航
```
返回(back) | 前进(forward) | 向上(up) | 向下(down)
首页(home) | 菜单(menu) | 关闭(close) | 更多(more)
```

### 操作
```
添加(add) | 删除(remove) | 编辑(edit) | 保存(save)
搜索(search) | 刷新(refresh) | 下载(download) | 上传(upload)
分享(share) | 链接(link) | 复制(copy) | 打印(print)
```

### 媒体
```
播放(play) | 暂停(pause) | 上一首(prev) | 下一首(next)
音量(volume) | 静音(mute) | 全屏(fullscreen) | 录制(record)
相机(camera) | 图片(image) | 视频(video) | 麦克风(microphone)
```

### 通信
```
电话(phone) | 邮件(email) | 消息(message) | 通知(bell)
定位(location) | 全球(globe) | WiFi | 蓝牙(bluetooth)
```

### 用户
```
用户(user) | 用户组(users) | 头像(avatar) | 登录(login)
注册(register) | 锁(lock) | 钥匙(key) | 验证码(verify)
```

### 文件
```
文件(file) | 文件夹(folder) | PDF | 图片(image)
文档(document) | 表格(spreadsheet) | 压缩包(archive) | 云端(cloud)
```
