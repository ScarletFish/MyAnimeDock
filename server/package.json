{
  "name": "anime-manager-server",
  "version": "1.0.0",
  "description": "MyAnimeDock backend server",
  "main": "dist/server.js",
  "bin": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "build": "pkg . --target node22-win-x64 --output ../src-tauri/resources/server.exe",
    "test": "tsc && node --test __tests__/*.test.js",
    "test:routes": "tsc && node --test __tests__/routes/*.test.js"
  },
  "pkg": {
    "assets": [],
    "scripts": [
      "dist/**/*.js"
    ],
    "targets": [
      "node22-win-x64"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "anitomy": "^0.0.35",
    "better-sqlite3": "^12.6.0",
    "pinyin": "^4.0.0"
  }
}
